# 律读 LuDoo · 技术设计文档

> 对应 PRD v1.0 · 架构 / 数据模型 / 关键实现

## 1. 总体架构

```
┌────────────────────────── 浏览器 ──────────────────────────┐
│  index.html(文献库)         reader.html(阅读器)              │
│  原生 ES Modules + CSS，无构建步骤                           │
│  api.js ──fetch──▶ REST API                                │
└──────────────┬─────────────────────────────────────────────┘
               ▼ HTTP (127.0.0.1:8765)
┌────────────────────────── FastAPI ─────────────────────────┐
│  routers：documents / annotations / search / ai              │
│  services：                                                 │
│   parsers/  pdf(PyMuPDF) docx(python-docx) web(trafilatura) │
│             text                                            │
│   legal/statute.py  法条正则识别 + 中文数字转换               │
│   llm.py             OpenAI 兼容 chat/completions（可选）    │
│  database.py  sqlite3（无 ORM）                              │
└──────────────┬─────────────────────────────────────────────┘
               ▼
   data/ludoo.db + data/uploads/   （全部本地）
```

**关键决策**
- **解析在服务端**：PDF/Word/网页解析质量最好且无浏览器跨域限制；法学课堂 Python 环境即可运行。
- **统一块结构**：四种来源都转为 `[{id, type: h1|h2|h3|p|quote, text, html}]`，阅读器只渲染——三源体验一致，批注锚点有稳定坐标系。
- **无构建前端**：ES Modules + 原生 CSS，`start.sh` 即起即用，符合个人工具的维护成本要求。
- **法条标注在导入时完成**：`statute.py` 生成带 `<span class="statute">` 的 html 存库；`text`（纯文本）与 `html` 同存——批注偏移按纯文本计算，渲染时对 `textContent` 生效，二者互不干扰。

## 2. 数据模型（SQLite）

```sql
documents(id, title, source_type, source_url, file_path, author, year,
          publication, tags(JSON数组), starred, blocks(JSON块列表),
          statutes(JSON法条清单), word_count, progress, ai_summary,
          created_at, updated_at)
annotations(id, document_id→documents ON DELETE CASCADE,
            type(重点|质疑|疑问|联想), quote, block_id, start, end,
            comment, created_at)
```

- 检索用 `LIKE` 子串匹配（标题/正文块/批注），个人库规模（百篇级）下可靠且零分词依赖；结果带前后 40/60 字上下文片段。
- 进度 `progress ∈ [0,1]`，前端就绪门闩后节流回写（见 §4）。

## 3. 解析管线

| 来源 | 库 | 要点 |
|---|---|---|
| PDF | PyMuPDF | 按 span 字号加权众数定正文字号 → 启发式识别标题；**双栏期刊**按页面中线分左右栏再按 y 排序拼接；页眉页脚（>40% 页面重复行/纯页码）过滤；提取字符 <80 判定扫描件并报错 |
| Word | python-docx | Heading 1-3 / Quote 样式映射；文档属性取标题作者 |
| 网页 | requests + trafilatura | UA 伪装、charset 自动探测（中文站点常不声明）；markdown 输出转块；标题清理（去 `--栏目--站点` 尾巴）；**页面家具清理**（订阅/收藏/字号/播报等按钮文字正则剔除） |
| 文本 | — | 空行分段；`#`/`##`/`###` 标题、`>` 引用的轻量约定 |

## 4. 批注锚点与渲染（前端核心）

- **创建**：`mouseup` → Selection API → `Range.toString().length` 计算块内偏移（`selectNodeContents(block)` 后 `setEnd(边界)`，天然规避 statute span 的文本节点分裂）→ `{block_id, start, end, quote}` 入库。
- **渲染**：加载时按块分组、`start` 降序从右向左包裹 `<mark class="hl" data-type>`；跨 statute span 的选区会拆成多个 mark（同一 annId），视觉连续。
- **防重叠**：创建前与既有批注做区间相交检查，重叠即提示。
- **进度门闩**：`progressReady` 标志在初始定位 + `requestAnimationFrame` 后才置位，防止加载期滚动事件把历史进度覆盖为 0（实测踩过的坑）。

## 5. 法条识别

```
正则：《法名》第[中文/阿拉伯数字]+(条(之N)?|编|章|节)
中文数字：千/百/十/零 权位累加（一千二百五十四→1254）
去重键：去掉「中华人民共和国」前缀的简称 + 条号 → 全称/简称合并计数
```

## 6. GB/T 7714 引用（前端 citation.js）

- 期刊 `[J]`：`作者. 题名[J]. 刊名, 年.`；网页 `[EB/OL]`：`作者. 题名[EB/OL]. (发布日)[引用日期]. URL.`；专著 `[M]` 类似
- 作者 3 人以上取前三 +「等」；支持实时编辑预览（信息面板字段联动重生成）
- 放在前端的原因：支持未保存的草稿字段即时预览，避免为每次键入请求后端

## 7. AI 辅助（可选）

- `.env`：`ZHIPU_API_KEY`（默认智谱 `glm-4-flash`，免费），可经 `LLM_BASE_URL`/`LLM_MODEL` 覆盖为任意 OpenAI 兼容服务
- 摘要按文献缓存于 `documents.ai_summary`；`GET /api/ai/status` 驱动前端能力开关与降级提示

## 8. API 一览

```
POST /api/documents/upload|url|text     导入（multipart / JSON）
GET/PATCH/DELETE /api/documents[/{id}]  列表(筛选q/tag/starred)/详情/改/删
GET /api/documents/{id}/annotations     批注列表
POST /api/documents/{id}/annotations    建批注（type/quote/block_id/start/end/comment）
PATCH/DELETE /api/annotations/{id}      改批注/删批注
GET /api/tags | /api/stats | /api/search?q=
GET /api/ai/status                      {configured, model}
POST /api/documents/{id}/summarize      AI 摘要（缓存）
POST /api/ai/explain                    划选解释
静态资源 Cache-Control: no-cache（改动刷新即生效）
```

## 9. 测试与质量

- **API 层**：curl 全链路（四种导入、批注 CRUD、检索命中正文/批注、标签统计、进度回写）
- **UI 层**：浏览器黑盒实测（IAB 自动化 + Chrome 视觉截图），覆盖 P0 流程与边界（空检索、未配 AI）
- **实测发现并修复**：批注锚点字段名不一致、空锚点致加载中断、进度覆盖竞态、引用漏标题、期刊格式标点、静态缓存、网页编码/家具文字等 7 项
- 样例数据：`scripts/make_samples.py` 生成本地演示文献（原创示意文本，含法条引用）
