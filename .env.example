# ===== AI 辅助功能配置（可选）=====
# 不配置 key 时，律读的所有本地功能（导入/阅读/批注/法条/检索/引用）完全可用，
# 仅 AI 摘要与划选解释两个增强按钮会提示"未配置"。
#
# 默认对接智谱开放平台（注册即有免费额度，glm-4-flash 免费）：
#   1. 复制本文件为 .env
#   2. 填入你的 API Key
#   3. 重启服务即可
#
ZHIPU_API_KEY=

# 也可以换成任意 OpenAI 兼容接口（OpenAI / DeepSeek / 本地 Ollama 等），
# 同时配置下面两项即可覆盖默认值：
# LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# LLM_MODEL=glm-4-flash

# ===== 公网访问口令（可选）=====
# 留空或不设置 = 仅本地使用，无需登录。
# 部署公网前请设置一个强口令：所有页面与 API 将要求先登录（浏览器记住 7 天）。
LUDOO_PASSWORD=
